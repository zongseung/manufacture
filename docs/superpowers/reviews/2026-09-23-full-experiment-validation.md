# KAMP 전체 예산 개발·9월 봉인 평가 기록 — 2026-09-23

작업 위치는 `manufacture_ai/.claude/worktrees/kamp-core`입니다. 먼저 9월 목표가 가려진 로더로 전체 개발 실험을 실행하고 `selection.json`을 고정했습니다. 이후 명시적 `--final`로 실제 2021-09-01~09-14 봉인 구간을 **한 번** 평가했습니다. 두 실행 모두 단일 RTX A6000(GPU 0)에서 종료 코드 0으로 끝났습니다. 커밋·푸시·외부 제출은 하지 않았습니다.

## 고정 선택과 개발 결과

선택 파일의 SHA-256은 `a138f8dac423e8940ac405f0adb2070efff56835efd385e172e0314dea2eba3a`입니다. 최종 성공 후에도 파일 바이트와 감사 기록의 해시가 같습니다. 제출 모델은 **B4 기본형**이며 전체 train 적합 epoch 300, 학습 구간에서 고정한 C50/C75/C90은 186.5/198.0/210.7입니다. H·IO·KAN을 포함한 4개 B4 후보를 모두 시도했고, 사전 지정한 MAE·평균 Brier 신뢰구간 게이트를 통과한 후보는 없었습니다. 게이트 실패 시 B4 계열 중 OOF MAE 최소를 고르는 규칙에 따라 기본형이 선택됐습니다.

| 후보 | 개발 OOF MAE | 개발 OOF 평균 Brier | 게이트 |
|---|---:|---:|---|
| B4 기본형 | 10.956 | 0.1497 | 실패 |
| B4-H | 13.234 | 0.1404 | 실패 |
| B4-IO | 11.813 | 0.1886 | 실패 |
| B4-KAN | 11.382 | 0.1493 | 실패 |

같은 5,016개 관측 슬롯에서 B1 OOF MAE는 13.904입니다. B4 기본형과 B1의 날짜 짝짓기 bootstrap(2,000회, 53일) ΔMAE는 −2.947, 95% CI는 **−6.415~+0.253**입니다. 평균 Brier 차이는 +0.00289, 95% CI는 −0.0663~+0.0691입니다. 둘 다 상한이 0 미만이라는 게이트 조건을 충족하지 못합니다. OOF 유효일은 53일, 일 피크 평가 가능일은 51일입니다. 같은 OOF에서 네 후보를 비교한 이 신뢰구간은 탐색적입니다.

## 한 번의 9월 평가

`results/test_predictions.csv`는 2021-09-01 00:00~09-14 23:45의 **1,344행·33열**이며 모든 발행 수치가 유한합니다. 제출 ID는 전 행 `B4`입니다. `eval_mask.csv`에는 관측 불가 슬롯 2개가 기록돼 있고, 점별 지표 분모는 1,342슬롯입니다. `final_started.json`과 `final_attempts.jsonl`은 성공한 시도 1회를 기록합니다.

| 9월 평가 지표 | B1 | 선택된 B4 | 분모 |
|---|---:|---:|---:|
| MAE | **6.526** | 10.538 | 1,342 슬롯 |
| CRPS | **5.073** | 8.077 | 1,342 슬롯 |
| 일 피크 MAE | **5.398** | 6.320 | 14일 |
| C90 Platt Brier | **0.0325** | 0.0384 | 14일 |

이 14일에는 실제 C90 초과 사건이 **0일**이어서 C90 AUC는 정의되지 않습니다. 개발 OOF와 달리 이 봉인 구간에서는 B1이 선택된 B4보다 오차가 낮았습니다. 이를 이유로 제출 변형을 사후 변경하거나 봉인을 다시 열지 않았습니다. 14일·사건 0건으로 전반적 모델 우월성이나 피크위험 판별력을 주장할 수 없습니다.

## 검증과 해석 범위

- 개발 완료 후 봉인 전: OOF 슬롯·일 표에 9월 날짜 없음, 79개 모델/변형/fold 그룹의 C50/C75/C90 `p*` 키 완비, isotonic 상태 `not_run`, 실제 final 파일 부재. 전체 검사 **140 passed**, Ruff·basedpyright 오류 0.
- 봉인 후: 같은 전체 검사 **140 passed**. 선택 SHA 불변, 완료 감사 시도 1회, 제출 열·날짜·모델 ID·유한값·분위수 및 위험 순서, 별도 평가 마스크를 확인했습니다. 결과 노트북 `03_results.ipynb`는 최종 `results/`를 읽어 오류 없이 실행·저장됐습니다.
- B1은 개발 OOF MAE 13.904로 B0′의 13.720보다 높아 사전 기술한 B1 튜닝 검토 조건이 발동했습니다. 이미 사용한 봉인 결과로 튜닝하거나 이번 모델을 다시 선택하지 않습니다.
- B4는 깊은 시간 인코더가 아니라 조건부 확률상태모형입니다. 결측 직후 AR 재시작은 근사이고, A+ 가동 여부는 계획 가용성을 가정한 과거 실현값 대용입니다. 실제 계획 정확도와 운영 효과는 별도 검증이 필요합니다.

주요 근거 파일은 `results/selection.json`, `metrics.csv`, `bootstrap.csv`, `test_metrics.csv`, `test_predictions.csv`, `eval_mask.csv`, `final_p_star.json`, `final_started.json`입니다. 제출 보고서 PDF·발표 PDF/PPT·설문 캡처·포털 제출은 이 실험 기록의 범위에 포함되지 않습니다.
